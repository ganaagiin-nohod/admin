export default function OverviewPage() {
  // This page component is required for the parallel routes to work
  // The actual content is rendered through the layout with parallel routes
  // Updated to fix Vercel deployment issue
  return null;
}
